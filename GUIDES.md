# 🚌 班车管理系统需求规格

## 一、系统整体架构

- **后端**：Spring Boot + Spring Data JPA + MySQL，提供 RESTful API
- **前端（Web）**：TypeScript + Ant Design Pro + UmiJS，集成开源地图组件（如高德/百度/MapLibre）
- **移动端（班车端）**：React Native（Android），负责 GPS 上报与晚点事件上报
- **移动端（乘客端）**：React Native（Android/iOS），支持扫码上车、站点提醒、订阅通知
- **消息中间件**：Kafka（用于发送乘客上车事件、考勤对接等异步通信）
- **审计日志**：所有关键操作（司机、运营管理员）需记录操作人、时间、变更内容

---

## 二、核心业务子域划分

### 1. 🚐 车辆管理子域（Vehicle Management）

**实体**：
- `Vehicle`（班车）
    - 品牌、车系、车型、车牌号
    - 类型（大巴 / 中巴 / 小巴）
    - 座位数
    - 供应商：供应商，例如“新朗途客运有限公司”

**用例**：
- 增删改查车辆信息
- 查询空闲车辆（用于排班）

---

### 2. 👨‍✈️ 司机管理子域（Driver Management）

**实体**：
- `Driver`
    - 姓名、性别、年龄、身份证号、联系电话、电子邮箱、所属公司
    - 驾照信息（驾照号、准驾车型、出生日期、初次领证时间、生效时间、到期时间）

**约束**：
- 驾照需在有效期内才能参与排班
- 只能与“空闲司机”调班

**用例**：
- 管理司机档案
- 查询可用司机（未排班、驾照有效）

---

### 3. 🗺️ 线路与站点管理子域（Route & Stop Management）

**实体**：
- `Route`（线路）
    - 名称、编码、描述、班车管理员（关联用户）
- `RouteDirection`（线路方向）
    - 方向（上行：目的地是公司 / 下行：出发地是公司）
    - 关联多个 `RouteStop`（按顺序）
- `PhysicalStop`（物理站点）
    - 名称、编码、描述
    - 多坐标（支持不同坐标系，如 WGS84、GCJ02）
    - 多张实拍图片（存储路径列表）

**聚合根**：`Route`（包含方向和站点序列）

**用例**：
- 创建/编辑线路及方向
- 管理物理站点（可被多条线路复用）
- 查询某线路的完整停靠序列与时间表

---

### 4. 👥 乘客管理子域（Passenger Management）

**实体**：
- `Passenger`
    - 姓名、员工编号、手机号
    - 类型（正式 / 派遣 / 借调）
    - 常坐线路及站点（多对多关系）
        - 其中一个为“默认主要偏好”（用于满载率与压力分析）

**用例**：
- 管理乘客信息
- 设置/更新默认乘车偏好
- 查询某线路的常乘客列表（用于统计）

---

### 5. 📅 排班管理子域（Scheduling Management）

**核心概念**：
- `Shift`（班次） = 某一天 + 某司机 + 某车辆 + 某线路方向

**用例**：
- 手动创建单个班次
- 基于模板批量生成排班计划（如每周一至五固定班次）
- 在日历视图中可视化编辑排班
- **调班（Shift Swap）**
    - 仅允许在“出发前”操作
    - 只能与“空闲司机”交换
    - 司机发起的调班 → 触发审批工单流程（需集成工作流引擎，如 Camunda 或自研状态机）
    - 运营管理员可直接调班（无需审批）

**约束**：
- 调班后原班次司机变为“空闲”，新司机占用该班次
- 所有调班操作记录审计日志

---

### 6. 📝 乘坐记录子域（Ride Record）

**实体**：
- `RideRecord`
    - 乘客 ID、班次 ID（即 Shift）
    - 上车时间（必填，由扫码触发）
    - 下车时间（可选，因下午不扫码）

**集成点**：
- 乘客扫码上车 → 向 Kafka 发送 `RideStartedEvent`
    - 用于 HR 考勤系统：“上车即打卡”

**用例**：
- 记录每次乘车行为
- 支持按日期/线路/乘客查询历史记录

---

### 7. 🚦 运营记录子域（Operation Log）

**实体**：
- `OperationLogEntry`
    - 班次 ID（Shift）
    - 物理站点 ID
    - 实际停靠时间、实际出发时间
    - （默认逻辑：出发时间 = 停靠时间 + 1 分钟，若未手动上报）

**数据来源**：
- 班车端 GPS 上报（含站点到达事件）
- 乘客扫码（间接推断车辆位置）

**用例**：
- 记录车辆在各站点的实际运行时间
- 用于后续准点率分析、调度优化

---

### 8. 📍 车辆定位与事件上报（Mobile Client Integration）

**班车端（Android）**：
- 定时自动上报 GPS 位置（低优先级）
- 司机手动上报 GPS（高优先级）
- 手动上报“晚点事件” → 触发考勤异常申报（对接 HR 系统）

**乘客端（Android/iOS）**：
- 扫描班车 QR Code（静态贴纸或动态码）
    - 触发上车事件 → 写入 `RideRecord` + 发送 Kafka 消息
- 订阅站点提醒
    - 用户选择关注某班次的某站点
    - 当车辆到达“前 N 站”（N 可配置，默认为 1）时，推送通知

**GPS 数据优先级规则**：
1. 司机手动上报（最高）
2. 乘客扫码时附带的 GPS（中，仅当有乘客上车）
3. 车机定时上报（最低）

> 系统应融合多源 GPS，以最高优先级为准更新车辆实时位置

---

### 9. 🔐 审计与安全（Cross-Cutting Concern）

**要求**：
- 所有司机、运营管理员的操作（增删改排班、调班、修改车辆/司机信息等）必须记录：
    - 操作人（用户 ID）
    - 操作时间
    - 操作类型
    - 修改前/后快照（或变更字段）
- 日志不可篡改，支持按时间/用户/操作类型查询

---

## 三、系统集成点

| 集成目标 | 触发条件 | 数据格式/协议 |
|--------|--------|-------------|
| HR 考勤系统 | 乘客扫码上车 | Kafka Topic: `ride_started` |
| HR 考勤系统 | 司机上报晚点 | REST API 或 Kafka |
| 地图展示（Web） | 实时车辆位置 | WebSocket / HTTP Polling |
| 站点提醒（乘客 App） | 车辆接近关注站 | FCM / APNs 推送 |

---

## 四、未来扩展考虑（可选标注）

- **满载率计算**：基于乘客默认偏好 + 实际乘坐记录
- **动态二维码**：班车端生成带时效/班次信息的动态码，防代刷
- **工单审批流程**：若使用 Camunda，需定义 BPMN 流程；否则可用状态机模拟